# Per-user data — never commit
ATHLETE.md
scripts/athlete.json
scripts/cache/
diary/

# Per-user agent settings + preferences (per-machine)
.claude/settings.local.json
PREFERENCES.md

# Python / OS noise
__pycache__/
*.pyc
.DS_Store
